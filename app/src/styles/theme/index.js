import { DefaultTheme } from 'styled-components'

import { redColors, purpleColors } from './colors'

const redTheme = { colors: redColors }

const purpleTheme = { colors: purpleColors }

export { redTheme, purpleTheme }
